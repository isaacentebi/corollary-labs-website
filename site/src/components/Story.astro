---
// The home page is one story on one stage, told at three zoom levels:
//   field (every node is a firm) → dive into one node → the statement beside the firm, figs 1–4 → pull back → diffusion, fig 5
//   → the whole lattice lifts into 3D and changes shape (homotopy coda).
// scripts/story.ts drives everything from one scroll progress.
import { site } from '../config/site';
// Captions: Set A from one clean-room writer (read the Superdark essay; no historical analogies), used verbatim.
// To be polished line by line with the founder.
export const figs: { n: number; title: string; lines: string[] }[] = [
  { n: 1, title: 'Supplied plans', lines: ['A firm is defined by its inputs and its outputs.', 'Each step executes a plan issued by coordination.'] },
  { n: 2, title: 'Agent as input', lines: ['The agent is supplied through the same entrance as other inputs.', 'It executes the existing plan. Layout and output are unchanged.'] },
  { n: 3, title: 'Objective input', lines: ['Step 2 receives an objective as its only control input.', 'The agent selects among alternative plans to meet it.'] },
  { n: 4, title: 'Redrawn boundary', lines: ['Execution and coordination are produced inside the boundary.', 'People work on the feedback loop and on outputs B and C.'] },
  { n: 5, title: 'Diffusion', lines: ['The same boundary change recurs across firms at uneven rates.', "The field's output mix shifts as each firm reorganises."] },
];
---
<section class="story" data-story aria-labelledby="hero-title">
  <h1 class="u-sr" id="hero-title">{site.name}</h1>
  <div class="story__stage" data-stage>
    <canvas class="field story__layer" data-field="hero" aria-hidden="true"></canvas>
    <canvas class="field story__layer" data-field="diffuse" aria-hidden="true"></canvas>
    <svg class="story__diagram story__layer" viewBox="0 0 1200 700" data-diagram role="img"
      aria-label={figs.map((f) => `Figure ${f.n}, ${f.title}. ${f.lines.join(' ')}`).join(' ')}>
      <defs>
        <filter id="boil" filterUnits="userSpaceOnUse" x="0" y="0" width="1200" height="700">
          <feTurbulence type="fractalNoise" baseFrequency="0.035" numOctaves="1" seed="1" data-boil />
          <feDisplacementMap in="SourceGraphic" scale="2.2" />
        </filter>
      </defs>
      <g data-dg><g filter="url(#boil)" data-ink></g></g>
    </svg>
    <canvas class="story__layer story__surface" data-surface aria-hidden="true"></canvas>
    <p class="story__statement">
      {site.name} is a company. <span class="ph">[Placeholder — one sentence on what it studies.]</span> <span class="ph">[Placeholder — one sentence on what it builds.]</span>
    </p>
    <div class="story__caption" aria-hidden="true">
      {figs.map((f, i) => (
        <div class="story__fig" data-fig={i}>
          <p class="story__n">FIG. {f.n} — {f.title.toUpperCase()}</p>
          {f.lines.map((l) => <p class="story__line">{l.toUpperCase()}</p>)}
        </div>
      ))}
    </div>
    <span class="story__cue t-label" aria-hidden="true" data-hero-scroll>↓</span>
  </div>
</section>
