---
// Inline term with a definition that opens softly on hover or focus.
interface Props { def: string }
const { def } = Astro.props;
---
<span class="term" tabindex="0"><slot /><span class="term__def" role="tooltip">{def}</span></span>
