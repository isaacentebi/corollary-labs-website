---
import '@fontsource/libertinus-serif/latin-400.css';
import '@fontsource/libertinus-serif/latin-400-italic.css';
import '@fontsource/libertinus-serif/latin-600.css';
import '@fontsource/libertinus-serif-display/latin-400.css';
import '@fontsource/libertinus-mono/latin-400.css';
import '../styles/global.css';
import Mark from '../components/Mark.astro';
import References from '../components/References.astro';
import { site, u, MATH_FONT } from '../config/site';

interface Props { title?: string; description?: string; page: 'home' | 'essays' | 'essay' | 'about' | 'team' | 'contact'; mark?: boolean }
const { title, description = site.description, page, mark = true } = Astro.props;
const fullTitle = title ? `${title} — ${site.name}` : site.name;
---
<!doctype html>
<html lang="en" data-page={page}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta name="theme-color" content="#f2eee5" />
    <link rel="icon" href={u('favicon.svg')} type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href={MATH_FONT} />
    <script is:inline>
      document.documentElement.classList.add('js');
      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) document.documentElement.classList.add('rm');
    </script>
  </head>
  <body id="top">
    <a class="skip" href="#main">Skip to content</a>
    {mark && <Mark />}
    <main id="main">
      <slot />
    </main>
    <References><slot name="refs" /></References>
  </body>
</html>
