---
// About: statement, a live plate of figure 4 (the reorganised firm), and the colophon set as a specimen label.
// Everything in [brackets] is a placeholder, to be written with the founder.
import Base from '../layouts/Base.astro';
import { site, figs } from '../config/site';
import '../styles/pages.css';

const rows = [
  { k: 'Entity', v: '[Entity]' },
  { k: 'Founded', v: '[Year]' },
  { k: 'Location', v: '[City]' },
  { k: 'Contact', v: site.email, href: `mailto:${site.email}` },
];
---
<Base page="about" title="About" description="[Meta description]">
  <article class="page about" aria-labelledby="about-title">
    <h1 class="page__title t-display" id="about-title" data-reveal>About</h1>
    <div class="about__grid">
      <p class="about__statement" data-reveal>
        <span class="ph">[About — one paragraph]</span>
      </p>
      <figure class="plate" data-reveal style="--rd:0.15s">
        <canvas data-plate="4" role="img" aria-label={`Figure 2, ${figs[1].title}`}></canvas>
        <figcaption class="t-label">Fig. 2, {figs[1].title}</figcaption>
      </figure>
      <section class="label" aria-label="Details" data-reveal>
        <dl>
          {rows.map((r) => (
            <div>
              <dt>{r.k}</dt>
              <dd class:list={{ ph: r.v.startsWith('[') }}>{r.href ? <a class="link-u" href={r.href}>{r.v}</a> : r.v}</dd>
            </div>
          ))}
        </dl>
      </section>
    </div>
  </article>
  <script>
    import { initPlates } from '../scripts/plate';
    initPlates();
  </script>
</Base>
