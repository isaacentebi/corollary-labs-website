node_modules/
site/dist/
site/.astro/
.DS_Store
# methodology repo is its own git repo (github.com/per-simmons/clone-app-pat-pro-public)
_method/
# large raw extraction dumps and screenshots (regenerable with tools/*.mjs)
clone-workspace/*/02-extraction/**/*.all-styles.json
clone-workspace/*/02-extraction/all-styles.json
clone-workspace/*/02-extraction/motion/*.json
clone-workspace/*/02-extraction/fragments/*.dom.html

# reference-site captures stay local (not ours to publish)
clone-workspace/locomotive/
clone-workspace/superdark/
/research/
deploy/
.claude/worktrees/

# node_modules symlink in a worktree
site/node_modules
